# 引き継ぎ書 — 楽マッチAI トップLP 全面刷新（Claude → Codex/GPT）

作成: 2026-07-20 ／ 引き継ぎ理由: Claude の週次利用上限。**以降は Codex(GPT) 側で完遂する。**

---

## 0. まずこれを読む順番

1. この `HANDOFF_TO_CODEX.md`（現在地と残作業）
2. `LP_RENEWAL_BRIEF.md`（**実装指示書の本体。制約・採点基準・禁止事項が全部ここ**）
3. `LP_IMPROVEMENT_PROPOSAL_GPT.md`（前段でGPT-5.6-Sol自身が書いた診断・提案。実装の設計図）

---

## 1. 到達目標（森山さんの原文）

> 「100点になるまで。UI・UX・デザイン・内容すべて100点になるまで。」

4軸すべて100点。採点基準と運用は `LP_RENEWAL_BRIEF.md` §5.5 に定義済み。
**採点の水増しは最悪の失敗。規制違反の100点は0点。**

---

## 2. 現在地

- 作業ブランチ: **`lp-renewal-gpt`**（master ではない）
- コミット履歴:
  - `6d1fa14` docs: 提案書と図解プロンプト集
  - （最新）`wip(lp): GPT-5.6-Sol によるLP全面刷新（作業途中・未検証）`
- **未 push**。origin には上がっていない。
- **未デプロイ**。本番 rakumatch-ai.com は旧LPのまま。

### すでに手が入ったファイル
```
next.config.ts / src/app/globals.css / src/app/layout.tsx / src/app/page.tsx
src/app/privacy/page.tsx
src/components/lp/Analytics.tsx
src/components/lp/layout/{Footer,Header,StickyCta}.tsx
src/components/lp/sections/{Faq,FinalCta,Hero,Pricing,Problem,Security,Story}.tsx
src/components/lp/site.ts / src/components/lp/ui/Reveal.tsx / src/lib/track.ts
```
### 新規作成
```
src/components/lp/sections/CapabilitySuite.tsx
src/components/lp/sections/SignalFlow.tsx
```

### ★重要: この状態は一度も検証されていない
`npm run build` を通していない。**壊れている可能性が高い。まずビルドを通すところから。**

---

## 3. 残作業

1. `npm run build` を通す（`tsc -b && next build`）。**通らない状態で先へ進まない。**
2. 未着手のセクションを含め、LP全体を `LP_IMPROVEMENT_PROPOSAL_GPT.md` の設計で完成させる。
3. **実際に描画して確認**（375px / 768px / 1440px）。横スクロールなし・文字14px以上・タップ44px以上。
4. 4軸採点 → 100点未満なら直す → 再採点。繰り返す。
5. `LP_RENEWAL_NOTES.md` に変更点を記録（何を、なぜ、どのセクションを廃止/新設したか）。

### 採点の第三者役について
指示書には Claude Sonnet 5 を批評役に使う案があるが、**Claude が週次上限のため当面使えない**。
GPT-5.6-Terra / Luna の採点役を複数立てて代替すること。使えない旨は最終報告に書く。

---

## 4. 絶対に破ってはいけない線（詳細は BRIEF §5）

- **お客様の声を創作しない。** `Voices.tsx` の8件は実在発言。本文改変・数値追加は禁止（配置とデザインの変更は可）。
- **効果数値を出さない**（「○倍」「○%削減」「成約率○%」）。
- **最上級表現を使わない**（「唯一」「他にない」「No.1」「業界初」）。価格の強さは**事実の列挙**で表す。
- **実在の競合サービス名を出さない**（「主要5社」「A社/B社」＋調査主体・時点・範囲の注記）。
- **法定ページ（`/privacy` `/terms` `/tokusho`）とフッター導線を壊さない。**
- 森山さんの写真・実名・「現役不動産営業／開発者」は実在資産。架空の人物写真に置き換えない。
- **`git push` しない。`master` に触らない。`vercel --prod` しない。** ← 森山さんが実物を見て判断する。

---

## 5. 技術上の落とし穴

- Next.js **16.2.1** / React **19.2.4**。`AGENTS.md` の警告どおり**学習データの Next.js とは違う**。
  `node_modules/next/dist/docs/`（391ファイル）の該当ガイドを読んでから書く。
- `src/components/lp/lib/headline.tsx` は Safari の和文語中改行対策。理由を読んでから触る。
- `magick`（ImageMagick）は**この母艦に入っていない**。画像処理に使おうとして失敗した記録あり。
  必要なら Node の sharp 等、リポジトリ内にある手段を使う。

---

## 6. 森山さんが確定済みの事業判断（再確認不要）

1. **1社目の主役 = 個人〜1〜3名の営業チーム**
2. **実物証拠 = 「今あるもので」**。新規の撮影・動画収録はしない。
3. **初期利用者への直接支援 = 約束してよい**。人数・範囲・連絡方法を具体案として決め、報告時に確認事項として上げる。
4. **個人導入時の顧客データ方針 = LPで言及しない。まっすぐ個人訴求する。**
   森山さん原文:「エージェントは個人でも顧客管理している人が多いはず。会社的にはいやかも。でもチームで導入する場合はトップが管理できるようにする。だが実際しっかり管理できるものは世の中にないと思う。だから言及しなくていい。普通に個人訴求していい。」

### 訴求の核（森山さん指定）
「安い」単独ではなく **「価格 × 機能量 × 1名から始められる」の密度**。
> 個人の裁量で入れられる月3,000円/人に、顧客管理・物件一括取り込み・双方向マッチング・専属AI・お客様アプリが全部入る。競合5社はいずれも価格非公開・商談から。

---

## 7. 完了したら森山さんに報告すること

- 新しいセクション構成（順序と役割）／廃止したセクションとその理由
- 4軸の最終スコアと採点の内訳・何周回したか・満点に届かない項目があればその差分
- 初期支援の約束をどう書いたか（人数・範囲・連絡方法）＝**森山さんの確認事項**
- ビルド結果
- 事実確認・法務確認が必要な箇所

森山さんは非エンジニアの経営者。**日本語で、専門用語には必ずかみ砕いた説明を添える。**
